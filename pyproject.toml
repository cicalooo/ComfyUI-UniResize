[project]
name = "comfyui-uniresize"
description = "Unified image resizing and compact model-aware resolution control."
version = "0.3.0"
requires-python = ">=3.10"

[tool.comfy]
DisplayName = "ComfyUI-UniResize"
