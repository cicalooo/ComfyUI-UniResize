[project]
name = "comfyui-uniresize"
description = "Unified image loading, resizing, and compact model-aware resolution control."
version = "0.4.0"
requires-python = ">=3.10"

[tool.comfy]
DisplayName = "ComfyUI-UniResize"
